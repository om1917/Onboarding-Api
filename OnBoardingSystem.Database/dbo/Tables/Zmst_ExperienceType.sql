CREATE TABLE [dbo].[Zmst_ExperienceType] (
    [id]             VARCHAR (2)   NOT NULL,
    [ExperienceType] VARCHAR (500) NULL,
    CONSTRAINT [PK_Zmst_ExperienceType] PRIMARY KEY CLUSTERED ([id] ASC)
);

