CREATE TABLE [dbo].[Zmst_TypeofDisability] (
    [id]          VARCHAR (5)   NOT NULL,
    [description] VARCHAR (250) NOT NULL,
    CONSTRAINT [PK_Zmst_TypeofDisability] PRIMARY KEY CLUSTERED ([id] ASC)
);

